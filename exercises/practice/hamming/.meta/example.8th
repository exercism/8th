: empty-string? \ s -- T
	s:len 0 n:= 
;

: distance \ s1 s2 | n
	\ if len s1 is zero 
	\ 	if len s2 is zero 
	\ 		return zero
	\	return null
	empty-string? 
	if 
	 	swap empty-string? 
		if \ both empty
			drop drop 0 ;; 
		then
		drop drop null ;; 
	then

	s:len -rot 
	swap
	s:len -rot
	2swap n:=
	!if drop drop null ;then

	null s:/ swap null s:/
	0 -rot 
	( s:= not nip if n:1+ then ) a:2each 
	2drop
;
